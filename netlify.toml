[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Next.js 16 is auto-detected by Netlify (OpenNext adapter).
# Do NOT add @netlify/plugin-nextjs here — Netlify installs it automatically.
